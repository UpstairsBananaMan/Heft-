/**
 * Every user-facing product name. brand.json mirrors this for app.config.
 * This file stays JSON-free so edge functions can import it.
 */
export const APP_NAME = "Heft";
export const APP_WORDMARK = APP_NAME.toLowerCase();
