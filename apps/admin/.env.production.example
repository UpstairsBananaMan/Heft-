# Hosted Supabase for the admin site. Copy to the host's environment, not into git.
# Use the same project as the phone app. Anon key only. Never put the service role here.

NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
